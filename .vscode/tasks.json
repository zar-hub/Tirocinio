{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "LoadMacro",
        "type": "shell",
        "command": "${workspaceFolder}/load_macro.sh",  // Replace with the actual path to your script
        "windows": {
          "command": "${workspaceFolder}/load_macro.bat"  // Replace with the actual path to your script
        },
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }
  